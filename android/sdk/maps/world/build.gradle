plugins {
  id 'com.android.library'
}

apply from: "${rootProject.rootDir}/groovy/common-config.gradle"

android {
  namespace = 'app.ecuamaps.sdk.maps.world'
  // enableKotlin = true  // see app/build.gradle
}
